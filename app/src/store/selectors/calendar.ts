import type { RootState } from '../index';

export const getCalendar = (state: RootState) => state.calendar;
// export const getReservations = (state: RootState) => state.calendar.currReservations;
// export const getView = (state: RootState) => state.calendar.view;
